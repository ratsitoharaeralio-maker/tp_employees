**maintenant compris**
    // ?? est l'opérateur de "coalescence des nuls" (null coalescing operator, PHP 7+).
    // Il signifie : "prends $_GET['dept_no'] s'il EXISTE et n'est PAS null, sinon prends ''".
    // Cela évite un warning "Undefined array key" si l'URL ne contient pas le paramètre.
    // Équivaut à : isset($_GET['dept_no']) ? $_GET['dept_no'] : ''

    number_format = pour bien ecrire un nombre

    ceil = arrondir la reponse d'une fraction (retourne la plus grande valeur ex 5.64 retourne 6)

    &larr; &rarr;

    function max

    urlencode

    round

    DATEDIFF

    if en mysql : (IF(condition, valeur_si_vrai, valeur_si_faux))

**n'avez pas encore compris**

**fonctions utilisées que vous ne connaissez pas**
